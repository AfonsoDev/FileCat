version: '3.8'

services:
  FileCat:
    build: .
    container_name: FileCat_app
    ports:
      - "8080:80"
    volumes:
      - ./storage:/var/www/html/storage
      - ./auth:/var/www/html/auth
      - ./messages:/var/www/html/messages
    restart: unless-stopped
